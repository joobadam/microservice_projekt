# Database Configuration
DB_NAME=urlshortener
DB_USER=postgres
DB_PASSWORD=password
DB_HOST=postgres
DB_PORT=5432

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Application Configuration
NODE_ENV=production
BASE_URL=http://localhost:5001
REDIRECT_TYPE=302

# Grafana Configuration
GRAFANA_PASSWORD=admin

# Optional: Custom ports (if you want to change defaults)
# FRONTEND_PORT=3000
# SHORTENER_PORT=5000
# REDIRECT_PORT=5001
# ANALYTICS_PORT=5002
# NGINX_PORT=80
# PROMETHEUS_PORT=9090
# GRAFANA_PORT=3001

# Optional: External database (if not using Docker)
# EXTERNAL_DB_HOST=your-db-host
# EXTERNAL_DB_PORT=5432
# EXTERNAL_DB_NAME=urlshortener
# EXTERNAL_DB_USER=postgres
# EXTERNAL_DB_PASSWORD=your-password

# Optional: External Redis (if not using Docker)
# EXTERNAL_REDIS_HOST=your-redis-host
# EXTERNAL_REDIS_PORT=6379
# EXTERNAL_REDIS_PASSWORD=your-redis-password
